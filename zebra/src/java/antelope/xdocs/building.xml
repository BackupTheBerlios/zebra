<?xml version="1.0"?>
<document>
	<properties>
		<title>Zebra Antelope - Building</title>
		<author email="bengidley@tigris.org">Ben Gidley</author>
	</properties>
	<body>
		<section name="Introduction">
			<p>Antelope uses <a href="http://maven.apache.org">Maven</a> to control it's build process. This is because it makes it easier!</p>
			
		</section>
		<section name="The Goals">
			<subsection name="antelope:setup-webapp">
				<p>This goal should be run if you change the project.xml. It will set up eclipse and 
					copy all the jars marked <code>war.bundle=true</code> to WEB-INF\lib folder.</p>					
			</subsection>
			<subsection name="antelope:setup-database">
				<p>This goes should be run to regenerate the database. Before running it make sure your database is running!</p>
				<p>This will</p>
				<ol>
					<li>Generate Hibernate files for XDoclet enabled hibernate 
						classes </li>
					<li>Drop your database and regenerate it</li>
					<li>Run in any admin data specified in a named unit 
						test suite</li>
				</ol>
			</subsection>
			<subsection name="site:generate">
				If you want to build this site and the javadoc etc run this goal.
			</subsection>
		</section>
		<section name="Configuration">
			<p>Firstly you should not have to reconfigure the build - but if 
				you want to, edit project.properties. It is pretty self 
				explanatory</p>
		</section>
	</body>
</document>